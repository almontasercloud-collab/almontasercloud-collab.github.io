---
title: ICMPipe
date: 2026-07-03 12:00:00 +0300
categories: [Experiments, ICMPipe]
tags: [ICMPipe, networking, Network Engineering, Computer Networks, Golang, ICMP, Packet Processing, Protocol Engineering, Packet Capture, gopacket, libpcap]
image:
  path: /assets/img/post_covers/ICMPipe.png
pin: false
---

## ICMP and the ICMPipe experiment

The **Internet Control Message Protocol (ICMP)** is best known for powering the `ping` command. It was designed for diagnostics and error reporting, not for general-purpose file transfer.

**ICMPipe** is a proof-of-concept that moves file contents through ICMP payload bytes by adding a lightweight application-layer protocol on top of standard ICMP echo request/reply traffic.

This article explains the protocol design, shows how the client/server exchange works, and highlights the practical limitations and improvement opportunities.

## Protocol overview

`ICMPipe` uses four payload flags:

- `FR` — File Request
- `FA` — File Acknowledgment
- `FP` — File Pull
- `FD` — File Data

The protocol runs in two phases:

1. Client sends `FR:<absolute path>` to request a file.
2. Server replies with `FA:<file size>` or `FA:File Not Found`.
3. Client sends `FP:<absolute path>` to begin the transfer.
4. Server streams the file in `FD:<base64 chunk>` payloads.
5. Client reassembles chunks until the total received size matches the acknowledged file size.

```bash
Phase 1: File discovery
Client -> Server: FR:<absolute_path>
Server -> Client: FA:<file_size> OR FA:File Not Found

Phase 2: File transfer
Client -> Server: FP:<absolute_path>
Server -> Client: FD:<base64_chunk>
Client -> Server: FD:<acknowledgment>
...
Terminate when received size >= file size
Reassemble payloads
```

## Current implementation and limitations

The current `ICMPipe` implementation is intentionally simple. It demonstrates the concept, but it does not yet offer the reliability or robustness required for production-grade transfer.

Key limitations include:

- Loss recovery
  - No retransmission for lost `FD` packets
  - No sequence numbers for out-of-order delivery
- Data integrity
  - Only file size is validated
  - No checksum, hash, or authentication
- Session recovery
  - No resume for interrupted transfers
- Packet handling
  - Assumes packets arrive in order
  - Does not manage ICMP fragmentation or device filtering
- Performance
  - Fixed payload chunk size
  - No congestion or rate control

### Enhancement opportunities

Recommended improvements:

- Add sequence numbers for every `FD` chunk.
- Add checksums or HMACs for chunk verification.
- Implement retransmission and timeout handling.
- Add resume support for interrupted transfers.
- Support adaptive chunk sizes and rate limiting.
- Add optional encryption or authentication for secure use.

## ICMPipe in action

`ICMPipe` depends on standard ICMP echo request/reply behavior. If the network or host modifies ICMP handling, the code may need adjustment and a rebuild.

Source code, executables, and installation instructions are available in the [GitHub repository](https://github.com/almontasercloud-collab/ICMPipe).

### Test lab workflow

A Linux-based client (`172.16.2.11`) requests a file from a Windows server (`172.16.2.102`). The experiment validates the `FR`/`FA`/`FP`/`FD` exchange, payload encoding, and packet-level behavior.

A second stage increases the ICMP payload size and repeats the experiment with a binary image file to demonstrate how the same protocol behaves with larger data and segmentation requirements.

## Running the ICMPipe server

For the initial test, only the Windows server executable is required.

1. Open Command Prompt as Administrator to allow raw ICMP operations.

![CMD as Administrator](/assets/img/posts_photos/icmpipe/cmd_as_admin.png)

2. Download the server executable:

```bash
curl -L https://raw.githubusercontent.com/almontasercloud-collab/ICMPipe/main/server/ICMPipe-Server.exe -o ICMPipe-Server.exe
```

![download ICMPipe server](/assets/img/posts_photos/icmpipe/server_download.png)

3. Enumerate available adapters and supported flags:

```bash
ICMPipe-Server.exe
```

![run ICMPipe server](/assets/img/posts_photos/icmpipe/run_server.png)

4. Start the server on the desired interface and bind it to the client IP:

```bash
ICMPipe-Server.exe 3 172.16.2.11
```

![server flags](/assets/img/posts_photos/icmpipe/server_flags.png)

> The server is now ready to handle incoming file requests from the configured client.

## Starting the ICMPipe client

1. Download the client binary:

```bash
wget https://raw.githubusercontent.com/almontasercloud-collab/ICMPipe/main/client/ICMPipe-Client
```

![Client Download](/assets/img/posts_photos/icmpipe/client_download.png)

2. Make the binary executable:

```bash
sudo chmod 775 ./ICMPipe-Client
```

![Make the Binary Executable](/assets/img/posts_photos/icmpipe/client_prvs.png)

3. Display client usage:

```bash
sudo ./ICMPipe-Client
```

![server flags](/assets/img/posts_photos/icmpipe/client_options.png)

## Initiating a file request

The logs below show the full transfer lifecycle from initial file request through final reassembly. The client IP is `172.16.2.11` and the server IP is `172.16.2.102`.

1. Confirm the requested file exists on the server:

![server flags](/assets/img/posts_photos/icmpipe/test_txt.png)

2. Request `test.txt` from the client:

```bash
sudo ./ICMPipe-Client -p "C:\Users\Administrator\Desktop\test.txt" -i "eth0" -ip 172.16.2.102 -O ./test.txt
```

![server flags](/assets/img/posts_photos/icmpipe/txt_file_request.png)

> The message `File downloaded and reassembled successfully in :  ./test.txt` indicates that the transfer finished successfully.

3. Verify the downloaded file:

```bash
cat ./test.txt
```

![server flags](/assets/img/posts_photos/icmpipe/test_txt_verify.png)

> Successful reconstruction confirms the file was transferred and reassembled correctly.

## Server runtime logs

The server log output shows how the request is processed and how file chunks are dispatched.

![server flags](/assets/img/posts_photos/icmpipe/ICMPipe_server_log.png)

## Packet capture analysis

The packet capture confirms the protocol behavior on the wire, from the initial `FR` request through the stream of `FD` chunks.

### File Request packet

![server flags](/assets/img/posts_photos/icmpipe/txt_first_packet.png)

- Payload (Base64):

```bash
RlJDOlxVc2Vyc1xBZG1pbmlzdHJhdG9yXERlc2t0b3BcdGVzdC50eHQ=
```

- Decoded payload:

```bash
FRC:\Users\Administrator\Desktop\test.txt
```

> The client sends `FR` followed by the requested path to start the transfer.

### File Acknowledgment packet

![server flags](/assets/img/posts_photos/icmpipe/txt_second_packet.png)

- Payload (Base64):

```bash
RkE4NjhGQWFiY2RlZmdoaWprbG1ub3BxcnN0dXZ3YWJjZGVmZw==
```

- Decoded payload:

```bash
FA868FAabcdefghijklmnopqrstuvwabcdefg
```

> The server replies with `FA<size>FA` and fills the remaining payload with default ICMP data.

### File Pull packet

![server flags](/assets/img/posts_photos/icmpipe/txt_third_packet.png)

- Payload (Base64):

```bash
RlBDOlxVc2Vyc1xBZG1pbmlzdHJhdG9yXERlc2t0b3BcdGVzdC50eHQ=
```

- Decoded payload:

```bash
FPC:\Users\Administrator\Desktop\test.txt
```

> The client sends `FP` to tell the server to begin streaming the file.

### Data chunk packets

![server flags](/assets/img/posts_photos/icmpipe/data_chunks.png)

- Payload:

```bash
FDSUNNUGlwZSBUZXN0IEZpbGUNCj09PT
```

- Decoded payload after removing `FD`:

```bash
ICMPipe Test File
===
```

> The server sends Base64-encoded file chunks prefixed with `FD`. The client decodes each chunk and assembles the file until transfer completion.

> The current chunk size is fixed in the server source code and can be adjusted here:
{: .prompt-tip }

```go
const icmpPayloadSize = 32  // Payload size
const prefix = "FD"
const usableDataSize = icmpPayloadSize - len(prefix)
count := 0

for i := 0; i < len(encodedData); i += usableDataSize {
    end := i + usableDataSize
    ...
}
```

## Conclusion

`ICMPipe` demonstrates a simple file transfer protocol built on ICMP echo messages. It shows how custom protocol logic can coordinate structured data exchange over a protocol originally designed for diagnostics.

This proof-of-concept is a strong foundation for future work. The next steps should focus on reliability, integrity, session recovery, adaptive payload handling, and protection from ICMP rate limiting and packet loss.
